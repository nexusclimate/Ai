[build]
  command = "npm run build"
  publish = "out"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Form submission notifications: configure in Netlify UI (Forms → Form notifications).
# See docs/FORM_NOTIFICATIONS.md for variables (%{formName}, %{siteName}, %{submissionId})
# and Reply-to (uses form field name="email").
[build.environment]
  # Optional: set if you use env in notifications
  # NOTIFY_EMAIL = "contact@nexusclimate.ai"
